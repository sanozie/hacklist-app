import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        type: 'dark',
        primary: { main: "#9D67E3" },
        secondary: { main: "#50E3C1" },
        tertiary: { main: "#FFD100" }
    }
});

export default theme